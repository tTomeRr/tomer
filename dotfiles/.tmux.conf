
# Enable mouse support
set -g mouse on

# Start windows at 1 instead of 0
set -g base-index 1

# Split panes with | and -
bind | split-window -h
bind - split-window -v

# Reload config
bind r source-file ~/.tmux.conf
